# {{APP_NAME}} Environment Configuration
# Generated: {{TIMESTAMP}}

# Application
PYTHONUNBUFFERED=1
PORT={{WEB_PORT}}
WEB_CONCURRENCY={{WEB_WORKERS}}
WEB_TIMEOUT={{WEB_TIMEOUT}}
SECRET_KEY={{SECRET_KEY}}
CLIENT_URL={{CLIENT_URL}}
NEWSROOM_WEBSOCKET_URL={{WEBSOCKET_URL}}
DEFAULT_TIMEZONE={{DEFAULT_TIMEZONE}}

# Auth
AUTH_SERVER_SHARED_SECRET={{AUTH_SERVER_SHARED_SECRET}}
AUTHLIB_INSECURE_TRANSPORT=1

# Database
MONGO_URI={{MONGO_URI}}
ELASTICSEARCH_URL={{ELASTICSEARCH_URL}}
ELASTICSEARCH_INDEX={{ELASTICSEARCH_INDEX}}
CONTENTAPI_ELASTICSEARCH_INDEX={{ELASTICSEARCH_INDEX}}
CONTENTAPI_ELASTIC_INDEX={{ELASTICSEARCH_INDEX}}
REDIS_URL={{REDIS_URL}}
CELERY_BROKER_URL={{REDIS_URL}}
CACHE_TYPE=redis

# Content API — shared with CMS (Superdesk writes, Newsroom reads)
# This MUST point to the same database as CMS's CONTENTAPI_MONGO_URI
CONTENTAPI_MONGO_URI={{CONTENTAPI_MONGO_URI}}

# Content Push — CMS pushes content to Newsroom via HTTP POST to /push
NOTIFICATION_KEY={{NOTIFICATION_KEY}}
PUSH_KEY={{PUSH_KEY}}

# Celery
SUPERDESK_CELERY_PREFIX=
APIPORT={{NEWSAPI_PORT}}
MGMTPORT={{MGMTAPI_PORT}}

{{OPTIONAL_VARS}}
